# Default values for nginx.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
service:
  url: demo.icp.ibm.com
  name: libertysimple
  type: NodePort
  externalPort: 9080
  internalPort: 9080
  nodePort: 32233
replicaCount: 1
image:
  repository: libertysimple
  tag: 1.3.0
  pullPolicy: IfNotPresent
deploy: private
